name: CI/CD Pipeline (Expo) - Fast

on:
  push:
    branches:
      - main
      - staging
      - develop
  pull_request:
    branches:
      - main
      - staging
      - develop

jobs:
  build:
    name: Build & Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      # Cache do npm, node_modules e Expo
      - name: Cache Node & Expo
        uses: actions/cache@v3
        with:
          path: |
            ~/.npm
            ~/.expo
            node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Install EAS CLI
        run: npm install -g eas-cli

      - name: Run tests
        run: npm test

      # Cache de EAS Build Artifacts (reduz upload/download do projeto)
      - name: Cache EAS Build Artifacts
        uses: actions/cache@v3
        with:
          path: |
            .eas/build
          key: ${{ runner.os }}-eas-build-${{ hashFiles('**/*') }}
          restore-keys: |
            ${{ runner.os }}-eas-build-

      - name: EAS Build Android
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: eas build --platform android --non-interactive --profile production
